package tester;

import java.sql.Connection;
import java.sql.ResultSet;
import java.sql.Statement;

import static utils.DBUtils.getDBConnection;

public class TestStatement {

	public static void main(String[] args) {
		// display all the details using emp
		//create a empty statement object to hold query in try with resource along with connection
		try(Connection conn = getDBConnection();
				Statement st = conn.createStatement();
				//also i can use select query : public ResultSet executeQuery(String sql) throws SQLExc
				ResultSet res = st.executeQuery("select * from my_emp");
				) {
			
		}catch (Exception e) {
			e.printStackTrace();
		}

	}

}
